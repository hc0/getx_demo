name: getx_demo
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  get: 4.6.5
  get_storage: ^2.0.3
  # 自适应分辨率
  flutter_screenutil: 5.1.0
  pull_to_refresh: ^2.0.0
  # 滚到指定的index（https://pub.dev/packages/scrollable_positioned_list）
  scrollable_positioned_list: ^0.3.1
  # 轮播（https://github.com/TheAnkurPanchani/card_swiper）
  card_swiper: 2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:

  uses-material-design: true

  assets:
    - assets/png/
